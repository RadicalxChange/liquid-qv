import type { LiquidQVProps } from '../types';

// Phase 0 stub — fully implemented across phases 1–4.
export const LiquidQV = (_props: LiquidQVProps = {}) => {
  return (
    <div className="p-8 text-center">
      <p className="font-display text-size-2">Liquid QV</p>
      <p className="text-size--1 text-gray">Scaffolded. Build is Phase 1+.</p>
    </div>
  );
};
