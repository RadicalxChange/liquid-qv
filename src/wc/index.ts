// Web Component build entry point.
//
// Wraps <LiquidQV /> as a custom element registered as <liquid-qv>.
// Built as an IIFE bundle; consumers drop it in via a <script> tag
// and use the element anywhere — no React on the host page required.
//
// Filled in in Phase 5.

export {};
